# Python 의존성 패키지

# 비동기 HTTP 클라이언트
httpx>=0.27.0

# YAML 파싱
pyyaml>=6.0.1

# 데이터 분석
pandas>=2.2.0

# GPU 모니터링
nvidia-ml-py3>=7.352.0

# 명령줄 인터페이스 (Python 표준 라이브러리에 포함되어 있지만 명시)
# argparse - 표준 라이브러리

# 비동기 프로그래밍 (Python 3.11+에 포함)
# asyncio - 표준 라이브러리

# JSON 처리 (Python 표준 라이브러리에 포함)
# json - 표준 라이브러리
