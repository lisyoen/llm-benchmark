# 벤치마크 대상 엔드포인트 설정
# localhost의 LiteLLM 서버를 대상으로 테스트합니다.

targets:
  # 로컬 LiteLLM 서버 (기본)
  - name: localhost
    base_url: http://localhost:4000/v1
    api_key: "test-key"
    description: "로컬 LiteLLM 서버"

# 기본 대상 (미지정 시 사용)
default_target: localhost
